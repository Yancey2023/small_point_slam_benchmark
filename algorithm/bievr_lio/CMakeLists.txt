set(_source "${CMAKE_CURRENT_SOURCE_DIR}/source/BIEVR-LIO")
if(NOT EXISTS "${_source}/CMakeLists.txt")
    message(FATAL_ERROR "Run scripts/download_algorithms.py bievr_lio and scripts/patch_algorithms.py apply bievr_lio")
endif()
add_subdirectory("${_source}" bievr-lio)
