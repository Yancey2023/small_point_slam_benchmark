set(_source "${CMAKE_CURRENT_SOURCE_DIR}/source/faster-lio")
if(NOT EXISTS "${_source}/CMakeLists.txt")
    message(FATAL_ERROR "Run scripts/download_algorithms.py faster_lio and scripts/patch_algorithms.py apply faster_lio")
endif()
add_subdirectory("${_source}" faster-lio)
