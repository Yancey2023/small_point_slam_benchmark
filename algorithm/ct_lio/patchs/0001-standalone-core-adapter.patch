diff --git a/CMakeLists.txt b/CMakeLists.txt
old mode 100755
new mode 100644
index 3e15930..a7a33a2
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,56 +1,58 @@
-cmake_minimum_required(VERSION 3.2)
-project(ct_lio)
-
-MESSAGE(STATUS "[Release Mode]")
-set(CMAKE_BUILD_TYPE "Release")
-
-# MESSAGE(STATUS "[Debug Mode]")
-# SET(CMAKE_BUILD_TYPE "Debug")
-# SET(CMAKE_CXX_FLAGS_DEBUG "$ENV{CXXFLAGS} -O0 -Wall -fpermissive -g2 -ggdb")
-
-# set(CMAKE_CXX_STANDARD 14)
-# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -Wuninitialized")
-
-ADD_COMPILE_OPTIONS(-std=c++14 )
-ADD_COMPILE_OPTIONS(-std=c++14 )
-set( CMAKE_CXX_FLAGS "-std=c++14 -O3" )
-set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -fexceptions" )
-set(CMAKE_CXX_STANDARD 14)
-set(CMAKE_CXX_STANDARD_REQUIRED ON)
-set(CMAKE_CXX_EXTENSIONS OFF)
-set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14 -pthread -std=c++0x -std=c++14 -fexceptions")
-
-find_package(catkin REQUIRED COMPONENTS
-  geometry_msgs
-  nav_msgs
-  sensor_msgs
-  roscpp
-  rospy
-  rosbag
-  std_msgs
-  tf
-  livox_ros_driver  ## for livox
-  eigen_conversions
-  message_generation
+cmake_minimum_required(VERSION 3.24)
+
+find_package(Eigen3 CONFIG REQUIRED)
+find_package(Ceres CONFIG REQUIRED)
+find_package(PCL REQUIRED COMPONENTS common filters kdtree)
+find_package(OpenCV REQUIRED COMPONENTS core)
+find_package(glog CONFIG REQUIRED)
+find_package(gflags CONFIG REQUIRED)
+find_package(OpenMP REQUIRED)
+
+add_library(ct_lio_algorithm STATIC
+  src/common/timer/timer.cc
+  src/common/utility.cpp
+  src/common/utils_trans.cpp
+  src/common/utils.cpp
+  src/common/global_flags.cc
+  src/algo/static_imu_init.cc
+  src/liw/lidarFactor.cpp
+  src/liw/lio_utils.cpp
+  src/liw/lio/lidarodom.cpp
 )
-add_definitions(-DROOT_DIR=\"${CMAKE_CURRENT_SOURCE_DIR}/\")
-
-
-
-
-catkin_package(
-  CATKIN_DEPENDS geometry_msgs nav_msgs roscpp rospy std_msgs
-  DEPENDS EIGEN3 PCL Ceres 
-  INCLUDE_DIRS include
+target_include_directories(ct_lio_algorithm PUBLIC
+  src
+  thirdparty
+  thirdparty/sophus
+  thirdparty/tessil-src/include
+  ${PCL_INCLUDE_DIRS}
+  ${OpenCV_INCLUDE_DIRS}
 )
-
-include(cmake/packages.cmake)
-include_directories(
-  include 
+target_link_libraries(ct_lio_algorithm PUBLIC
+  Eigen3::Eigen Ceres::ceres glog::glog gflags
+  ${PCL_LIBRARIES} ${OpenCV_LIBS} yaml-cpp::yaml-cpp
 )
-
-include_directories(${PROJECT_SOURCE_DIR})
-include_directories(${PROJECT_SOURCE_DIR}/src)
-include_directories(${PROJECT_SOURCE_DIR}/thirdparty/sophus)
-
-add_subdirectory(src)
\ No newline at end of file
+if(OpenMP_CXX_FOUND)
+  target_link_libraries(ct_lio_algorithm PUBLIC OpenMP::OpenMP_CXX)
+  target_compile_definitions(ct_lio_algorithm PUBLIC
+    MP_EN
+    MP_PROC_NUM=${SLAM_BENCHMARK_PARALLEL_THREADS}
+    SLAM_BENCHMARK_PARALLEL_THREADS=${SLAM_BENCHMARK_PARALLEL_THREADS}
+  )
+else()
+  target_compile_definitions(ct_lio_algorithm PUBLIC
+    MP_PROC_NUM=1
+    SLAM_BENCHMARK_PARALLEL_THREADS=1
+  )
+endif()
+
+add_executable(ct_lio_benchmark standalone/ct_lio_adapter.cpp standalone/main.cpp)
+target_link_libraries(ct_lio_benchmark PRIVATE
+  ct_lio_algorithm slam_benchmark::core spdlog::spdlog
+)
+if(MSVC)
+  target_compile_options(ct_lio_algorithm PRIVATE /W3 /utf-8)
+  target_compile_options(ct_lio_benchmark PRIVATE /W4 /utf-8)
+else()
+  target_compile_options(ct_lio_algorithm PRIVATE -Wall -Wextra)
+  target_compile_options(ct_lio_benchmark PRIVATE -Wall -Wextra -Wpedantic)
+endif()
diff --git a/src/liw/lio/lidarodom.cpp b/src/liw/lio/lidarodom.cpp
index 8b2e820..9d06c87 100644
--- a/src/liw/lio/lidarodom.cpp
+++ b/src/liw/lio/lidarodom.cpp
@@ -7,6 +7,7 @@ namespace zjloc
 
      lidarodom::lidarodom(/* args */)
      {
+          time_curr = 0.0;
           laser_point_cov = 0.001;
           current_state = new state(Eigen::Quaterniond::Identity(), Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero(), Eigen::Vector3d::Zero());
 
@@ -16,6 +17,9 @@ namespace zjloc
 
           index_frame = 1;
           points_world.reset(new pcl::PointCloud<pcl::PointXYZI>());
+          pub_cloud_to_ros = [](std::string &, CloudPtr &, double) { return true; };
+          pub_pose_to_ros = [](std::string &, SE3 &, double) { return true; };
+          pub_data_to_ros = [](std::string &, double, double) { return true; };
      }
 
      lidarodom::~lidarodom()
@@ -25,6 +29,11 @@ namespace zjloc
      void lidarodom::loadOptions()
      {
           auto yaml = YAML::LoadFile(config_yaml_);
+          options_.parallel_threads = yaml["parallel_threads"]
+              ? yaml["parallel_threads"].as<int>()
+              : SLAM_BENCHMARK_PARALLEL_THREADS;
+          if (options_.parallel_threads <= 0)
+               throw std::runtime_error("parallel_threads must be positive");
           options_.surf_res = yaml["odometry"]["surf_res"].as<double>();
           options_.log_print = yaml["odometry"]["log_print"].as<bool>();
           options_.max_num_iteration = yaml["odometry"]["max_num_iteration"].as<int>();
@@ -76,7 +85,9 @@ namespace zjloc
           options_.thres_translation_norm = yaml["odometry"]["thres_translation_norm"].as<double>();
      }
 
-     bool lidarodom::init(const std::string &config_yaml)
+     bool lidarodom::init(const std::string &config_yaml,
+                          const Vec3d &lidar_translation_in_imu,
+                          const Mat3d &lidar_rotation_in_imu)
      {
           config_yaml_ = config_yaml;
           StaticIMUInit::Options imu_init_options;
@@ -85,22 +96,14 @@ namespace zjloc
 
           auto yaml = YAML::LoadFile(config_yaml_);
           delay_time_ = yaml["delay_time"].as<double>();
-          // lidar和IMU外参
-          std::vector<double> ext_t = yaml["mapping"]["extrinsic_T"].as<std::vector<double>>();
-          std::vector<double> ext_r = yaml["mapping"]["extrinsic_R"].as<std::vector<double>>();
-          Vec3d lidar_T_wrt_IMU = math::VecFromArray(ext_t);
-          Mat3d lidar_R_wrt_IMU = math::MatFromArray(ext_r);
-          std::cout << yaml["mapping"]["extrinsic_R"] << std::endl;
-          Eigen::Quaterniond q_IL(lidar_R_wrt_IMU);
-          q_IL.normalized();
-          lidar_R_wrt_IMU = q_IL;
+          // LiDAR-to-IMU calibration is dataset metadata supplied by the
+          // benchmark initialization interface, not an algorithm parameter.
+          Eigen::Quaterniond q_IL(lidar_rotation_in_imu);
+          q_IL.normalize();
           // init TIL
-          TIL_ = SE3(q_IL, lidar_T_wrt_IMU);
-          R_imu_lidar = lidar_R_wrt_IMU;
-          t_imu_lidar = lidar_T_wrt_IMU;
-          std::cout << "RIL:\n"
-                    << R_imu_lidar << std::endl;
-          std::cout << "tIL:" << t_imu_lidar.transpose() << std::endl;
+          TIL_ = SE3(q_IL, lidar_translation_in_imu);
+          R_imu_lidar = q_IL.toRotationMatrix();
+          t_imu_lidar = lidar_translation_in_imu;
 
           //   TODO: need init here
           CT_ICP::LidarPlaneNormFactor::t_il = t_imu_lidar;
@@ -199,6 +202,15 @@ namespace zjloc
           }
      }
 
+     void lidarodom::processAvailable()
+     {
+          auto measurements = getMeasureMents();
+          for (auto &measurement : measurements)
+          {
+               ProcessMeasurements(measurement);
+          }
+     }
+
      void lidarodom::ProcessMeasurements(MeasureGroup &meas)
      {
           measures_ = meas;
@@ -209,9 +221,6 @@ namespace zjloc
                return;
           }
 
-          std::cout << ANSI_DELETE_LAST_LINE;
-          std::cout << ANSI_COLOR_GREEN << "============== process frame: "
-                    << index_frame << ANSI_COLOR_RESET << std::endl;
           imu_states_.clear(); //   need clear here
 
           // 利用IMU数据进行状态预测
@@ -375,11 +384,7 @@ namespace zjloc
                ceres::LossFunction *loss_function = new ceres::HuberLoss(0.5);
                ceres::Problem::Options problem_options;
                ceres::Problem problem(problem_options);
-#ifdef USE_ANALYTICAL_DERIVATE
-               ceres::LocalParameterization *parameterization = new RotationParameterization();
-#else
-               auto *parameterization = new ceres::EigenQuaternionParameterization();
-#endif
+               auto *parameterization = new ceres::EigenQuaternionManifold();
 
                switch (options_.icpmodel)
                {
@@ -480,7 +485,7 @@ namespace zjloc
 
                ceres::Solver::Options options;
                options.max_num_iterations = 5;
-               options.num_threads = 3;
+               options.num_threads = options_.parallel_threads;
                options.minimizer_progress_to_stdout = false;
                options.trust_region_strategy_type = ceres::TrustRegionStrategyType::LEVENBERG_MARQUARDT;
 
@@ -1105,7 +1110,6 @@ namespace zjloc
                eskf_.SetInitialConditions(options, imu_init_.GetInitBg(), imu_init_.GetInitBa(), imu_init_.GetGravity());
                imu_need_init_ = false;
 
-               std::cout << ANSI_COLOR_GREEN_BOLD << "IMU初始化成功" << ANSI_COLOR_RESET << std::endl;
           }
      }
 
diff --git a/src/liw/lio/lidarodom.h b/src/liw/lio/lidarodom.h
index 1177892..cd8262d 100644
--- a/src/liw/lio/lidarodom.h
+++ b/src/liw/lio/lidarodom.h
@@ -2,7 +2,6 @@
 #define LIDAR_ODOM_H__
 #include "common/timer/timer.h"
 #include "liw/lidarFactor.h"
-#include "liw/poseParameterization.h"
 
 #include "algo/eskf.hpp"
 #include "algo/static_imu_init.h"
@@ -15,9 +14,6 @@
 #include "common/utility.h"
 #include "tools/point_types.h"
 
-#include <sys/times.h>
-#include <sys/vtimes.h>
-
 namespace zjloc
 {
 
@@ -26,6 +22,7 @@ namespace zjloc
           double surf_res;
           bool log_print;
           int max_num_iteration;
+          int parallel_threads = 1;
           //   ct_icp
           IcpModel icpmodel;
           double size_voxel_map;
@@ -67,12 +64,15 @@ namespace zjloc
           lidarodom(/* args */);
           ~lidarodom();
 
-          bool init(const std::string &config_yaml);
+          bool init(const std::string &config_yaml,
+                    const Vec3d &lidar_translation_in_imu,
+                    const Mat3d &lidar_rotation_in_imu);
 
           void pushData(std::vector<point3D>, std::pair<double, double> data);
           void pushData(IMUPtr imu);
 
           void run();
+          void processAvailable();
 
           int getIndex() { return index_frame; }
 
@@ -188,4 +188,4 @@ namespace zjloc
      };
 }
 
-#endif
\ No newline at end of file
+#endif
diff --git a/src/tools/point_types.h b/src/tools/point_types.h
index 302118f..0252294 100644
--- a/src/tools/point_types.h
+++ b/src/tools/point_types.h
@@ -11,9 +11,7 @@
 
 #include "common/eigen_types.h"
 
-#include <livox_ros_driver/CustomMsg.h>
-
-namespace zjloc
+namespace zjloc
 {
 
     // 定义系统中用到的点和点云类型
diff --git a/standalone/ct_lio_adapter.cpp b/standalone/ct_lio_adapter.cpp
new file mode 100644
index 0000000..aba8bd0
--- /dev/null
+++ b/standalone/ct_lio_adapter.cpp
@@ -0,0 +1,118 @@
+#include "ct_lio_adapter.hpp"
+
+#include <slam_benchmark/calibration.hpp>
+#include <spdlog/spdlog.h>
+
+#include <algorithm>
+#include <chrono>
+#include <cmath>
+#include <ranges>
+#include <stdexcept>
+
+namespace {
+double seconds(slam_benchmark::TimestampNs value) {
+    return static_cast<double>(value) * 1e-9;
+}
+
+slam_benchmark::TimestampNs nanoseconds(double value) {
+    return static_cast<slam_benchmark::TimestampNs>(std::llround(value * 1e9));
+}
+}  // namespace
+
+slam_benchmark::InitializationResult CtLioAdapter::initialize(
+    std::span<const slam_benchmark::SensorDefinition> sensors,
+    const std::filesystem::path& config_path,
+    slam_benchmark::ResultSink& sink) {
+    auto compatibility = slam_benchmark::check_dataset_compatibility(
+        sensors, {.lidar = true, .imu = true, .point_time = true});
+    if (!compatibility) return compatibility;
+    const auto lidar = compatibility.selected_sensor(sensors,
+                                                      slam_benchmark::SensorType::Lidar);
+    const auto imu = compatibility.selected_sensor(sensors,
+                                                    slam_benchmark::SensorType::Imu);
+    if (lidar == sensors.end() || imu == sensors.end()) {
+        throw std::runtime_error("CT-LIO requires one LiDAR and one IMU");
+    }
+    sink_ = &sink;
+    const auto transform =
+        slam_benchmark::target_from_source(imu->calibration, lidar->calibration);
+    Vec3d translation(transform[3], transform[7], transform[11]);
+    Mat3d rotation;
+    rotation << transform[0], transform[1], transform[2],
+        transform[4], transform[5], transform[6],
+        transform[8], transform[9], transform[10];
+    odometry_.init(config_path.string(), translation, rotation);
+    std::function<bool(std::string&, SE3&, double)> pose_callback =
+        [this](std::string& topic, SE3& state, double timestamp) {
+            if (topic != "laser") return true;
+            const Eigen::Quaterniond quaternion(state.rotationMatrix());
+            slam_benchmark::Pose pose;
+            pose.timestamp_ns = time_origin_ns_ + nanoseconds(timestamp);
+            pose.translation_m = {state.translation().x(), state.translation().y(),
+                                  state.translation().z()};
+            pose.rotation_xyzw = {quaternion.x(), quaternion.y(), quaternion.z(), quaternion.w()};
+            trajectory_.push_back(pose);
+            sink_->report_realtime_pose(pose);
+            return true;
+        };
+    odometry_.setFunc(pose_callback);
+    spdlog::info("CT-LIO standalone adapter initialized");
+    return compatibility;
+}
+
+void CtLioAdapter::process(const slam_benchmark::SensorSample& sample) {
+    if (time_origin_ns_ == 0) time_origin_ns_ = sample.timestamp_ns;
+    const double sample_time = static_cast<double>(sample.timestamp_ns - time_origin_ns_) * 1e-9;
+    const auto started = std::chrono::steady_clock::now();
+    if (const auto* imu = std::get_if<slam_benchmark::ImuSample>(&sample.payload)) {
+        odometry_.pushData(std::make_shared<zjloc::IMU>(
+            sample_time,
+            Eigen::Vector3d(imu->angular_velocity_rad_s[0], imu->angular_velocity_rad_s[1],
+                            imu->angular_velocity_rad_s[2]),
+            Eigen::Vector3d(imu->linear_acceleration_m_s2[0], imu->linear_acceleration_m_s2[1],
+                            imu->linear_acceleration_m_s2[2])));
+        const double elapsed = std::chrono::duration<double, std::milli>(
+            std::chrono::steady_clock::now() - started).count();
+        sink_->report_timing({sample.timestamp_ns, "imu_preprocess", elapsed});
+    } else if (const auto* cloud = std::get_if<slam_benchmark::PointCloud>(&sample.payload)) {
+        if (cloud->points.size() != cloud->point_time_offset_ns.size()) {
+            throw std::runtime_error("point/time array size mismatch");
+        }
+        std::vector<point3D> points;
+        points.reserve(cloud->points.size());
+        const auto [minimum, maximum] = std::ranges::minmax(cloud->point_time_offset_ns);
+        const double span = std::max(1e-9, static_cast<double>(maximum - minimum) * 1e-9);
+        const double begin = sample_time + static_cast<double>(minimum) * 1e-9;
+        for (std::size_t index = 0; index < cloud->points.size(); ++index) {
+            const auto& source = cloud->points[index];
+            if (!std::isfinite(source.x) || !std::isfinite(source.y) || !std::isfinite(source.z)) continue;
+            point3D point;
+            point.raw_point = {source.x, source.y, source.z};
+            point.point = point.raw_point;
+            point.intensity = source.intensity;
+            point.ring = source.ring;
+            point.relative_time = static_cast<double>(cloud->point_time_offset_ns[index] - minimum) * 1e-9;
+            point.alpha_time = point.relative_time / span;
+            point.timespan = span;
+            point.timestamp = begin + point.relative_time;
+            points.push_back(point);
+        }
+        odometry_.pushData(std::move(points), {begin, span});
+        const double elapsed = std::chrono::duration<double, std::milli>(
+            std::chrono::steady_clock::now() - started).count();
+        sink_->report_timing({sample.timestamp_ns, "lidar_preprocess", elapsed});
+    }
+    const auto processing_started = std::chrono::steady_clock::now();
+    const auto poses_before = trajectory_.size();
+    odometry_.processAvailable();
+    if (trajectory_.size() > poses_before) {
+        const double elapsed = std::chrono::duration<double, std::milli>(
+            std::chrono::steady_clock::now() - processing_started).count();
+        sink_->report_timing({sample.timestamp_ns, "registration", elapsed});
+    }
+}
+
+void CtLioAdapter::finalize() {
+    odometry_.processAvailable();
+    sink_->report_final_trajectory(trajectory_);
+}
diff --git a/standalone/ct_lio_adapter.hpp b/standalone/ct_lio_adapter.hpp
new file mode 100644
index 0000000..3e07df7
--- /dev/null
+++ b/standalone/ct_lio_adapter.hpp
@@ -0,0 +1,20 @@
+#pragma once
+
+#include <liw/lio/lidarodom.h>
+#include <slam_benchmark/algorithm.hpp>
+
+class CtLioAdapter final : public slam_benchmark::SlamAlgorithm {
+public:
+    [[nodiscard]] std::string_view name() const noexcept override { return "ct_lio"; }
+    slam_benchmark::InitializationResult initialize(std::span<const slam_benchmark::SensorDefinition> sensors,
+                    const std::filesystem::path& config_path,
+                    slam_benchmark::ResultSink& sink) override;
+    void process(const slam_benchmark::SensorSample& sample) override;
+    void finalize() override;
+
+private:
+    slam_benchmark::ResultSink* sink_{};
+    zjloc::lidarodom odometry_;
+    slam_benchmark::TimestampNs time_origin_ns_{};
+    slam_benchmark::Trajectory trajectory_;
+};
diff --git a/standalone/main.cpp b/standalone/main.cpp
new file mode 100644
index 0000000..bafbfdc
--- /dev/null
+++ b/standalone/main.cpp
@@ -0,0 +1,8 @@
+#include "ct_lio_adapter.hpp"
+
+#include <slam_benchmark/application.hpp>
+
+int main(int argc, char** argv) {
+    CtLioAdapter algorithm;
+    return slam_benchmark::run_benchmark_main(argc, argv, algorithm);
+}
